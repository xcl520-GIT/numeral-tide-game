# 更新日志

这个项目的每个版本都以同一件事收尾：**重跑平衡模拟 → 重跑集成冒烟 → 重建发行版**。
所以下面每一条都是"能跑、且被验证过"的状态，而不是"改了一堆东西"。

---

## v11.10 —— 指标常驻化 · 前两层搬家 · 给防御一个对手

- **「后半程压力」进了 `sim.html` 的常驻指标行**。口径是条件概率
  （到过第 3 层的局里有多少死在第 3-4 层），从此不用每次改动都重写探针。
  两个坑在实现里堵住：通关局也写在 `byDepth` 里（不摘掉赢家，标准档会算出
  `(8+132)/140 = 100%`）；分母取 `endDepth>=3` 的全部而不是"第 3 层死亡 + 赢家"。
  ⚠ 记账：`n=300` 时它的 1σ ≈ **±4.2pp**，读这个数必须连着样本量。
- **深渊前两层：搬家，不是降难度**。补一条结构认识：第 1 层**不吃 `enemyScale`**，
  强度旋钮只有 数量/精英/补给 三个，而前两个 v11.8 已证伪
  ⇒ **补给是第 1 层唯一还剩的杠杆**。药方 `healAfterWin` .024→**.040**（= 标准档）
  + `deepCorrupt` .06→**.105**（两条是一组）。n=900：通关率 21.8% → 22.8%、
  **总死亡 704 → 695（守恒）**、第 1 层死亡 627 → 541、
  **到达第 3 层 241 → 305（+27%）**、第 3-4 层死亡 40 → **94**。
  复活流被否（通关率 35.3% 越档、总死亡不守恒、回合翻倍 —— 那是换游戏）。
- **装备堆防御那条债：给防御一个对手**。5 发探针拆成三层：症状不在了
  （三档 max defP = 111/139/**192**，够不到债里记的 231~299，900 局 0 stall）；
  防御是**生存支柱**（消融掉 8.0/8.3/7.0pp —— 注意"高防局全通关"是幸存者偏差，
  分开两者只能靠消融）；**怪物的穿透恒为 0**（`penP/penM` 一直原值透传，
  而模板没一个写它）。药方是**穿透**而不是抬攻击：伤害公式是饱和的，
  抬 atk 先杀低防玩家；穿透是减法，削防御的绝对量。
  实测它打在谁身上：低防桶承伤 6.1 → 5.9 **纹丝不动**，中防桶 2.4 → 3.0。
  落点 `abyss.penPerDepth: 8`（n=900）：−2.7pp、**第 1 层逐位不变**、
  装备防御的价值 6.9pp → **4.2pp**（仍有用，但不再是万能）。
- 平衡：**72.7 / 42.3 / 22.3**（休闲/标准**逐位未变**）｜ 冒烟 **291 项 ALL PASS**
  ｜ `probe_audit` 19/0 ｜ **0 stall / 0 timeout**。深渊 23.3% → 22.3%（净 −1.0pp：
  第 2 项 +1.7、第 3 项 −2.7），单局回合 211.2 → 237.5。
- **回退**：删掉 `data.js` 里 abyss 的 `penPerDepth: 8`；
  把 `healAfterWin` 改回 `0.024`、`deepCorrupt` 改回 `0.06`（后两条是一组）。
- 顺带修一处隐患：`smoke.html` 第 56 节原来把"6%"写死在断言里，
  `deepCorrupt` 一调值就 FAIL，而它报的样子和"机制坏了"一模一样 ——
  现在从数据表读。

---

## v11.9 —— 把难度搬到后半程：「深层的潮水」

- **根因**：深渊的难度曲线"前重后空"（v11.8 量出：死在第 1-2 层 211 局、
  死在第 3-4 层只有 5 局）。本轮第二发探针找到了原因 ——
  **怪的伤害被"防御饱和 + 玩家血池暴涨"同时吃掉了**：伤害走 `rawDamage` 的
  饱和公式 `atk·K/(def+K)`，而玩家的血池是**纯收益**（第 1 层 168 → 第 5 层 808）。
  怪的**攻击**只涨 ×2.8，玩家的**血**涨 ×4.8。
  （血量那一侧是平衡的：怪血 ×4.0 vs 玩家血 ×4.8，所以"几下一只"稳定在 2 下 ——
  P5 的雪球结论在这里得到第二次印证。失衡的只有伤害这一侧。）
- **「提怪的攻击」这条路是死的**：给深层加一个攻击二次项，第 5 层乘到 **4.15 倍**，
  它占玩家血也只从 0.1% 涨到 0.6% —— 要到第 1 层那种压迫感得再乘 10 倍以上，
  而那会让第 3 层先爆掉。**每场掉血的绝对值根本没变**（第 1 层 9.2 血/场、
  第 5 层 7.8 血/场），变的只是分母。
- **药方**：第 3 层起，每场战斗后按**最大生命的 6%** 扣血 ——
  **无视防御的真实伤害**。它不是新机制：潮水腐蚀（踩水掉血）本来就是真实伤害，
  `core.js` 里明确写着"不走 rawDamage，无视一切防御"。
  按比例扣血是**唯一不会被血池稀释**的压力形式（血池翻倍，扣血也翻倍）。
- **结果**：新指标（到过第 3 层的局里有多少死在第 3-4 层）**5.6% → 20.2%**，
  后半程第一次真的有压力。⚠ 如实记账：深渊通关率 **28% → 23.3%**（−4.7pp）。
  **第 1、2 层逐位不变**（`{1:200, 2:11}`），连休闲/标准的遗物分布、掉落品质、
  死因分布都逐位相同。旁证：深渊 `deathBy` 里「潮水」从 19 涨到 32。
- **实现**：原 `_fightSettle` 改名 `_fightSettleCore`，外面包一层 `_fightSettle`
  再调 `_deepCorrupt()`。为什么包一层：`_fightSettleCore` 内部有 **4 个 return 分支**
  （打死 / 被打死 / 撤离 / 僵持），在每条分支前各补一次调用是最容易漏的做法。
- **冒烟 290 → 291 项**：断言同时盯三件事 —— 第 3 层扣对数额、第 2 层不扣、
  标准档不扣。第一条防的是"按**当前血**而不是最大血算"：那会让**残血时扣得少**，
  机制恰好在该起作用的时候失效（"残血反倒更安全"，与设计意图完全相反）。
- 平衡：**72.7 / 42.3 / 23.3** ｜ 冒烟 **291 项 ALL PASS** ｜ 0 stall / 0 timeout
- **回退**：删掉 `data.js` 里 `abyss` 的 `deepCorrupt: 0.06` 那一行（不写即旧行为）。

---

## v11.8 —— 深渊「顶部过重」的改判（**无代码改动**）

- 5 发探针把工作单那条欠了两轮的债判掉了：**「第 1 层死亡数」不是独立的病，
  它是深渊难度的温度计**（v11.4-f 39.7% 通关时是 157，现在 28% 时是 200，
  两者是同一件事的两面）。工作单的验收线 `<120` 是在 39.7% 那个前提下定的。
- **两次干净证伪**，指向同一个道理「敌人**既是威胁也是资源**」（每 4 杀升级、
  每 6 杀秘藏）：怪数砍半 → 第 1 层死亡 200 → **206（不动）**、
  通关率 28% → **15%（塌）**；关掉精英提前 → **210（更差）**；潮水放慢 → 完全不动。
- 唯一能达标的是给深渊复活（`extraLife 1` → 110），但通关率被推到 **43.3%**
  （高于标准档 42.3%）—— 深渊就不再是深渊。
- **真形状是「前重后空」**：`{1:200, 2:11, 3:4, 4:1, 5:84}` —— 前两层死 211 局、
  第 3、4 层只死 5 局。而 `enemyScale` +40% 只值 **−1pp**：**那里没有人会死**。
- → 指标改判为条件概率（本轮不动代码）；把难度搬到后半程成为 v11.9 的立项。

---

## v11.7 —— P5 重写立项与落地（真根因是「速度碾压」）

- **P5 的立项被重写：病根不在装备上。** v11.6 曾判"雪球没有滚起来"，
  但那一次是拿**跨局聚合**的三个数下的结论 —— 跨局聚合有**幸存者偏差**
  （只有活到第 5 层的局才贡献高层样本，而那些局的玩家本来就强）。
  这一轮改用**局内配对**：同一个局里第 5 层的每击伤害是第 1-2 层的
  **4.57 倍**（63 局里无一低于 2 倍），遗物从 0 件涨到 16 件 —— **雪球成立**，
  只是被敌人血线的层数缩放（×3.5）对冲掉了，所以没变成"一击一只"。
- **真根因：速度碾压的阈值。** `speedGap = 18` 的意思是"速度差 ≥18，快的一方每轮多打一次"，
  而游侠基础速度就是 26、杂兵多在 4~16 —— 开局就踩在这条线上。
  实测速度差 ≥18 的战斗**一轮结束率 76%**、<18 只有 **19.8%**（差 **56.2pp**，
  且在 18 处有一道台阶）。**"一轮"不等于"一次出手"**：速度碾压让一轮出手 2 次，
  而杂兵血线正好是"2 手"的量级。
- **三个药方候选，两个被量化否掉**：工作单推荐的"装备加成递减"实测无用
  （遗物 mods 整体 ±30% 只值 ±2.5pp）；"只给深渊的怪加速度"会把
  "怪碾压玩家"从 3.2% 抬到 10.8%、通关率 −6.6pp —— 只是把碾压换了个受害者。
- **采用**：只给深渊一个更高的阈值 —— `data.js` 的 `abyss.speedGap: 26`
  （写法是"可选字段，不写就用全局默认"，所以**删掉这一行即回退**）。
  休闲/标准**逐位相同**（连遗物分布、掉落品质、死因分布都逐位相同），
  深渊「一轮结束」**39.5% → 32.3%**（验收线 ≤35%，达标）。
  ⚠ 如实记账：深渊通关率 29.3% → **28.0%**（−1.3pp）、第 1 层死亡 188 → **200**。
- **⚠ P5 的第二条验收指标没有达标**（第 1 层死亡 <120 @n=300），
  而且它与 P5 的药方**方向相反** —— 压速度碾压 = 玩家变弱 = 前期死更多。
  建议从 P5 拆出去，与 P4"深渊顶部过重"合起来单独立项。
- **冒烟 289 → 290 项**：新增"速度差 20 时标准 2 次 / 深渊 1 次"。
  这条断言当场抓到了"新变量定义了、使用处忘了换"这个洞 —— 没有它，
  症状会是"改了跟没改一样"，而游戏照样能玩、三档只差几个 pp。
- 平衡：**72.7 / 42.3 / 28.0** ｜ 冒烟 **290 项 ALL PASS** ｜ `probe_audit` 19/0 ｜ 0 stall / 0 timeout

---

## v11.6 —— 撤离的 AI 判据重定 · 两个默认转正 · P5 重验

- **撤离的 AI 判据重定**。老判据"预期本轮承伤 > 剩余血 30%"实测 **22 次/局 ≈ 逢战必撤**、
  通关率 −19.6 / −15.0 / −13.7pp。根因是语义错了：它算的是"这一轮疼不疼"，
  不是"这一场会不会输" —— 上一轮的疼下一轮照样能打，撤了却把整场收益丢掉。
  改成**应急出口**：预期本轮承伤 ≥ 剩余生命就撤。再补上"撤完真的跑开"
  （脱离期 3 回合，往远离最近那只敌人的方向走）—— 少了它会出现**撤离循环**。
  结果：撤离降到 **4.7 / 1.1 / 1.1 次/局**，通关率 −0.6 / 0.0 / −2.3pp（1σ 以内）。
- **`duelcd` 默认转正**（冷却随战斗轮走，用户拍板）。⚠ 如实记账：它的影响比立项时估的更大 ——
  冷却走快之后 **AI 用魂技的次数翻了四倍**（542 → 2091 次），所以它值
  **+8.4 / +9.0 / +2.3pp**（不是单开时量的 +1.6/+6.6/+4.3）。
  这一档降得比较狠，`?duelcd=0` 一格就能回到 64.3 / 33.3 / 27.0。
- **`fleeai` 定稿为默认关**。判据重定、撤完脱离、撤离冷却三件都做了（机制与断言齐全），
  但实测 AI 主动撤**净亏**：cd=1 时 67.0 / 39.0 / 26.3 vs 不撤的 72.7 / 42.3 / 29.3
  （−5.7 / −3.3 / −3.0pp）。之前量到的"接近中性"是靠**没有冷却**换来的，
  而那正是免伤循环（终检那 1 例 timeout）的来源。机制全留，`?fleeai=1` 可复现。
- **终检逮到两个缺陷**（都是这一轮引入的、也都在这一轮修掉）：
  ① `_retreatStep` 只看"最近那只"敌人 → 身边一多方向来回翻，**两格震荡**走到回合上限；
     改成"势能严格下降"（对附近每只敌人算分数，只走更好的那格）—— 结构上排除震荡。
  ② **撤离没有冷却** → 每回合都撤 = 每回合都不挨打（脱接触护着那只怪）= 免伤循环。
     加 3 回合冷却，界面同步显示剩余回合。
- **P5（深渊装备雪球）重验后挂起**。立项依据那句话已经过期：
  当时是"一击 203 点 vs 第 5 层杂兵 200~240 血"，现在第 5 层杂兵血线**中位 407**
  （A1 把普通怪血量抬上去的直接后果），而玩家每击中位 102 / p90 294 ——
  **"一击秒一只"不再成立**，雪球没有滚起来。P5 的立项描述要重写，不能按原方案做。
- 平衡：**70.7 / 41.7 / 28.3**（休闲 / 标准 / 深渊）｜ 冒烟 **288 项 ALL PASS**
- 未跟踪遗留：`release` 包名此前一直写死 v11.1 —— 本次修正为 v11.6。

---

## v11.5 —— P0~P4 与 P3：从"能玩"到"每一轮都有决定"

- **P0 分职业量化**：四职业 × 三档矩阵进开发记录（此前只有体感没有数据）。
- **P1 让等待有代价**：`?waitcost=a|b`。等待从"零代价"变成要么推潮汐节拍、
  要么让半径内已醒的敌人免费挪一步；AI 同步学会"该不该等"（判据是魂技冷却差 1~3 回合，
  那是全游戏唯一随回合变好的量）。
- **P2 空间改造轮**：路宽 1→2 定稿（窄口 19% → 2%，野生群战率 14~17% → 24~26%）——
  三轮以来**唯一真的拉动了群战率**的杠杆。代价是三档 −3.0 / −8.3 / −2.3pp，
  那是一次**有意的难度上调**。旧地图没删：`?roadw=1` 回到改造前。
- **P4 Boss 行为差异化**：三个 Boss 各一条行为（潮汐之主缩短节拍 / 骨潮督军孵幼体 /
  渊喉推开玩家），挂在模板的 `bossBehavior` 上、总开关 `?bossfx=0|1`。
  途中逮到两个自己引入的缺陷：吼不看玩家在哪（2700 局里响了 2921 次）、
  吼能把玩家推进完全抵消的死循环（4 例 timeout）。
- **P3 第二、三步**：魂技进对决（对决里可挂一次"本轮效果"）+ 撤离（定价三件套 +
  脱接触）。菜单第 4、5 个动作（键 3 / 4）。
- 平衡：**64.3 / 33.3 / 27.0** ｜ 冒烟 **273 → 288 项**

---

## v11.2 ~ v11.4 —— 补记（本轮补写；数字取自当时的平衡基线档，不是现在重测的）

**v11.2（a~l，12 个提交）**
- 敌人标签体系 · 每层机制主题 · 敌人姿态轮换 · 意图预告
- 秘藏「针对词条」（19 → 22 件）· **伤害类型体系**（物理 / 法术 / 真实）
- **手动选物法 + 克制奖励** —— 把"引擎替玩家择优"拆掉，换回"让玩家自己算"
- HUD 出手开关 · 敌人信息面板从「给答案」改成「给题目」
- 战斗场景 + 逐轮回放 + 视觉三修 + 构图重做 + 背景从「洞窟」改成「房间」

**v11.3（a~d，4 个提交）**
- `_duel` 拆成**可中断的对决**（`newDuel/step/runAuto`），验收标准是**逐位相同**
- 逐回合战斗：每轮停下来选一路 + 姿态在战斗内轮换（模拟器 AI 同步改）
- 高分辨率战斗立绘（96×96 / 64×64 重绘，与地图立绘共用几何）
- 体检修复：背景闪烁 · 输入门禁漏三层 · 切屏残留 · 三处文案撒谎
- 当时读数：冒烟 **235 项** ｜ 三档 **79.3 / 52.7 / 38.0**

**v11.4（a~r）**
- 群战：踩上敌人格时把主角四邻一起拉进对决（最多 4 个）
- 守位与惊动半径 · 槽位指派 · 战斗中的世界时钟 · 群攻三形态（横扫）
- 打击感（命中顿帧 / 受击后坐按掉血比例 / 战斗界面震屏）· 战斗音效
- 开局魂技三选一（保底本职业技）· 层尾守门堆（A-lite）
- 三次干净证伪：**站位这一侧已经没有杠杆**（走法 / 出发时刻 / 速度全部证伪）
- 当时读数：冒烟 **267 项** ｜ 三档 **69.7 / 44.0 / 30.3**

---

## v11.1 —— 修掉两个卡死级 bug：秘藏选牌死锁 · 侧栏内容被裁

这一版没有新玩法，只修两个**真把人卡住**的问题。两个都是先复现、再修、再回归。

- **秘藏选牌死锁。** 秘藏共 19 件且不会重复获得，所以候选池一定会被抽干。
  抽干之后 `_offerRelics()` 返回**空数组**，而空数组在 JS 里是**真值** ——
  模型层 `if (this.pendingRelic)` 把它当成"正在等玩家选"，把移动与技能输入**永久拒掉**；
  界面层却用 `.length` 判断，得到"没有待选"，面板不弹。
  两个口径之间那条缝就是死锁：**点不动、也没有面板可关**。
  修法不是给某一处打补丁，而是把口径收敛到唯一的 `hasPendingRelic()`，
  并让候选**永远补满**（对齐《云顶之弈》的强化选择）——
  池子空了就换成「潮汐馈赠」（金币 / 回血 / 装备）三选一：候选永远三张，永远能选一张继续。
- **侧栏看不全 / 滚不动。** 侧栏是 flex 列，子块默认 `flex-shrink:1`；
  内容超高时**不是侧栏滚动，而是面板被压扁**，而每个 `.panel` 又带 `overflow:hidden` ——
  内容被静默裁掉、侧栏永远不溢出、滚动条永远不出现。
  「属性看不到」和「滚不动」不是两个 bug，是同一个。
  实测：1380×880 下 5 个面板共丢 **265px**、12 行属性里 **4 行**看不到；
  1120×720 下丢 **425px**、**7 行**看不到。
  修法：`#sidebar > * { flex: 0 0 auto; }`，让侧栏真的滚动；顺带把滚动条调亮
  （原 0.18 透明度在深色底上几乎看不见，这也是"以为滚不动"的一部分）。

回归（全部在改动之后重跑）：

| 项 | 结果 |
|---|---|
| 集成冒烟 · 源码 | **202 项全过 / 0 失败** |
| 集成冒烟 · 发行包副本 | **202 项全过 / 0 失败** |
| 平衡模拟 n=300 · 三档通关率 | 85% / 56.7% / 40.3% —— **与 v11.0 逐位相同** |
| 平衡模拟 n=300 · 深渊秘藏分布 | 19 项**逐位相同** |
| 选牌死锁复现 `tools\probe_relic.html` | 修复前：面板不弹 + 输入永久被拒；修复后：3 张卡、选完能继续走 |
| 侧栏取证 `tools\probe_sidebar.html` | 修复前：5 块被裁 265px；修复后：0 块被裁、可滚 283px |

> 顺手还抓到一个**模拟器自己**的问题：给补位卡打分时，它算出金币(40)/回血(55) 高于
> 最弱的两件秘藏（`batfang` 0.14 分、`gambler` −8.6 分），于是 AI 拿永久秘藏换了 40 金币 ——
> 深渊的秘藏分布整体偏掉 8%（那两件直接归零）。把基线改成"永久 > 一次性"后逐位复原。
> 教训：**平衡数据里的每一个异常都要追到机制，不能当噪声跳过。**

---

## v11.0 —— 区域化地图 · 无尽模式 · 本机排行榜 · 操作指南

- **区域化地图**：每层切成 4~6 个区域（多源 BFS 划分，保证连通且不重叠），
  类型有普通 / 精英 / 奖励 / 危险 / 守门，出口固定落在守门区。
  配对画面层：地面染色、区域边界、区域标签、小地图按区域上色、
  出口脉动门标、小地图上方的出口导航，以及进入新区域时的居中横幅。
- **第一层不设高难区**：开局那一层承担的是教学，不该开局 20 步就撞上硬仗。
- **无尽模式**：不新增难度旋钮，只把"到第 N 层结束"这条线拿掉，
  越过正篇后叠一档递减增量的强度曲线。
- **本机排行榜**：分数公式只有一个执行点；难度用乘法而非加法
  （有断言钉住"刷低难度永远不划算"）；通关奖励远高于多打一层。
- **操作指南**：按键表抽成 `data.js` 的唯一一份 `KEYS`，指南按当前界面
  过滤后渲染（局内 / 背包 / 选牌各一套），并且冒烟测试**逐条按下去**验证。
- 平衡重标定：休闲 85.0% / 标准 56.7% / 深渊 40.3%（每档 300 局）。
- 集成冒烟 166 → **202 项**。

## v10.8 —— 修「怎么卡住了」+ ESC 暂停菜单

- 玩家报"卡住"，真因是标题页内容 1132px 高于窗口 880px，**开始按钮被挤出了可视区**。
  结晶面板改成弹窗 + 操作区 `sticky`，并加了布局几何断言。
  （教训：`element.click()` 不关心元素在不在屏幕里，程序化点击永远成功。）
- ESC 暂停菜单：语义是**逐层往回退**，不是一键回主菜单。
- 新增 `probe_title.html` 布局取证工具（支持 `?w= ?h= ?page=`）。

## v10.7 —— 元进度（潮汐结晶）与 BGM

- 局外成长：8 项永久解锁，三层分离（数据表 / 存档 / 模型层只在显式传入时生效），
  从结构上杜绝"元进度污染平衡基准"。
- BGM：Web Audio 程序化音序器，零音频文件。提前排程而不是 `setInterval` 直接发声。

## v10.6 —— 把四个角色的技能真正做完

- 裂地斩（击退 + 震晕）、潮语洪流（潮湿）、疾影（免费行动 + 35% 增伤）、
  不退之壁（双防 ×2.5 + 反伤）。
- 每个技能一套独立的音色 / 施法姿态 / 招牌特效 —— 缺一层都会"没感觉"。

## v10.4 / v10.5 —— 试玩反馈驱动的修复

- 修「开局弹错」：兜底分级（资源型错误忽略 / 单个错误不停机 / 连续出错才停机）。
- 修「融合没有反馈」：事件推了但没人消费，新增 `TideMain.flush()`。
- 修「技能没感觉」：四层反馈一起补齐。

## v10.3 —— 地图错位（一次很有代表性的 bug）

世界地形层 `drawImage` 把"源偏移"当成了"目标偏移"，地形和角色错开整整一个相机位移，
视口左上还留出等宽黑带；而相机停在原点时错误恰好抵消。
表现为"地图是黑的、人物错位、偶尔走到某处又正常了"。

## v10.0 —— 目录重构

启动器移到项目最外层（双击即玩），最初的原型独立成 `旧版-数字吞噬/`，
`build.ps1` 改成"只删白名单 + 断言路径在项目根内"。

## v9 及以前

- v4 战斗模型：`伤害 = 攻击 × 50 / (有效防御 + 50)`，物法各算一遍取高。
- v5 潮汐：压力来自"窗口在关闭"，而不是"血条在漏"。
- v6 三档难度：发现**敌人强度不是难度旋钮**（正反馈循环会把它吃掉），
  改用"战后回血"重新标定。
- v1~v3 原型：从"数值吞噬"（吃掉比自己小的就变大）逐步演化到属性构筑。
