# 技术实现：六处不好做的地方，以及为什么这么做

> 这份文档讲**代码层面的难点与取舍**。全部对应仓库里的真实代码，
> 每节最后给落点（文件 / 函数），可以照着读。
>
> 规模：全套源码 11 个文件、13953 行 / 770 KB。核心 `core.js` 5424 行（纯逻辑，不碰 DOM）、
> 界面 `ui.js` 3119 行、数据表 `data.js` 1222 行、像素美术 `art.js` 1089 行。

---

## 一、零素材：美术全部由代码生成

**约束**：一个人做像素游戏，最容易死在美术上 —— 要么买素材（成本 + 风格不统一），
要么自己画（一个人画 8 方向 × 十几个角色 × 状态帧，等于再做一个项目）。
**而且这个项目连图片文件都不想要**：目标形态是"整个文件夹拷走双击就玩"。

**做法**：写一个**参数化像素生成器**，而不是画图。
角色与怪物都是一组参数（体型 / 宽高 / 眼数 / 配色 / 突起 / 手臂 / 王冠…），
绘制器按参数现场画；主角是分层纸娃娃（发型 / 身体 / 武器分别绘制后叠加）。

**几个具体取舍**：

- **同一形体，两种尺寸。** 地图上 32px、战斗界面 96px —— 不是两套图，
  而是同一个参形体按整数倍绘制（×3）。冒烟里有一条断言专门钉住这件事：
  *"英雄战斗立绘与小立绘**同一个形体**（背面/正面逐像素一致）"*。
  少了它，某天改一处绘制就会让两处悄悄分叉，而这种分叉肉眼很难发现。
- **不赌系统字体。** 战斗界面上的所有图标（意图、状态、增益）都是 canvas 路径画的 ——
  系统字体里有没有那个符号，不该由用户的机器决定。
- **图标同理**：来自 game-icons.net 的 CC BY 3.0 图标不落成 SVG 文件，
  而是**内联**进 `src/js/icons.js`（101 行、100 KB）—— 少一次网络请求、少一套文件管理。

**落点**：`src/js/art.js`（地形生成器 + 纸娃娃 + 参数化怪物）、`src/js/icons.js`、
`tools/pick_icons.py`。

---

## 二、地图：随机，但每一层都必须真的走得通

**难点**：程序化地图最怕的不是"不好看"，而是**偶尔生出一张走不通的图**。
这种缺陷在模拟里表现为"某几局莫名卡死"，靠调数值永远掩盖不掉。

**做法是三层**：

1. 撒房间（带上限重试，避免重叠）→ L 形走廊连通 → 清理水洼与岩浆。
2. **确定性兜底**：从玩家出生点做一次洪水填充；出口不可达就**直接凿一条 L 形应急通道**，
   并把走不到的敌人从场上移除。宁可地图上多一条笔直的走廊，也不能让这一层无法通过。
   这是"地图生错了不能靠调数值掩盖，必须有确定性兜底"这条纪律的来源。
3. **区域划分**：在地图上放若干种子，各自做多源 BFS —— 每个可走格归属最近的那个种子。
   种子的选法不是随机的，而是**最远点采样**（每次都挑离已有种子最远的格子），
   否则会切出一堆针尖状的小区域，既没玩法、也没法上色。

**区域的"玩法含义"**：出口所在的区固定是守门区；其余区按类型抽，且
**精英 / 危险 / 奖励 / 柱厅每层各最多一个** —— 全是高难区域等于没有规划空间。
第 1 层是教学层，不设任何高难与地形干扰。

**落点**：`core.js` 的 `_buildLevel()` / `_ensureConnectivity()` / `_reachable()` /
`_buildRegions()` / `_applyRegionFlavor()` / `_carvePillars()`。

---

## 三、一群怪怎么"围上来"，而不是排成一条线

**难点**：让每只怪各自寻路到玩家，数学上必然导致它们从同一个方向来 ——
因为目标格只有玩家四邻那 4 格，从同一侧出发的怪对 4 格的路径长度排序高度一致，
于是"理性"地选了同一个最优格。实测把这件事钉死了：目标格冲突率只有 1.7%
（看起来没排队），但**走得动却落不到玩家四邻的比例高达 86%**。

**做法**：加一层**外层分配**（槽位）——每回合先把"玩家四邻"分给不同的怪，
怪再各自寻路到**自己被分到的那个格子**。这就是"围而不堵"。
另外两条配套纪律：

- **回巢**：脱战后的怪要真的走回自己的巢位，否则玩家反复拉打几轮，堆形会永久散掉。
- **惊动半径**替换区域门禁：门禁的判据是"玩家已经踏进这个房间"，
  等于让怪比玩家晚出发整整一个房间 —— 攻击波次永远起不来。改成按距离惊动之后才正常。

**落点**：`core.js` 的 `_assignSlots()` / `_enemyGoal()` / `_stepEnemyTowardPlayer()` /
`_updateAwake()`、`data.js` 的 `PACK`。

---

## 四、寻路：水不是墙，区域约束交给寻路层

`findPath()` 是一个 A*，有三处设计值得单说：

1. **水的代价是 5，不是"不可通行"。** 于是"涉水抄近路 vs 绕开省血"变成真决策 ——
   只有在绕不过去时才会踩水，此时代价（血量）就变成了玩家要算的那笔账。
   敌人传 `waterCost = 0`（它们本来就不怕水）。
2. **`onlyRegion` 参数**：怪不许越区这条规则，由**寻路层**拒绝越界来实现，
   而不是散落在移动代码里写 `if`。散着写迟早会漏掉第二条移动路径 ——
   一个项目的规则应该只有一个执行点。
3. **步数上限是实测调出来的，不是拍的**：`_walkToward` 的 A* 上限从 4000 抬到 20000。
   依据：900 局里它失败 268 次，**全部**都是"目标格自身可走"（不是目标非法，是搜不到），
   抬到 20000 有 28.4% 能找到路，再抬到 60000 一次都不增加 —— 20000 就是"够的那个数"。

**落点**：`core.js` 的 `findPath()` / `_walkToward()`。

---

## 五、战斗数值：一个公式、一条饱和曲线、两种"对付防御"的手段

伤害只有**一个执行点**：`rawDamage(atk, pen, def)` = `atk·K/(def+K)`（K=50）。

**为什么是饱和曲线**：它让"堆防御"的边际收益递减但**永不归零** ——
既能防住"一件神装直接无敌"，又不会让防御变成废属性。

**穿透为什么是减法**（`eff = max(0, def − pen)`）：抬攻击是在**分子**上加，
而公式是饱和的 —— 实测第 5 层把 atk 抬到 ×4.15，玩家每场掉血占最大生命只从 0.1% 涨到 0.6%
（先死的是低防玩家）。穿透是在**分母**上做减法，削的是防御的**绝对量**，
是唯一能"对抗防御"而不是"绕过防御"的手段。深渊的每层穿透 +8 就是这么来的，
实测把"装备防御的价值"从 6.9pp 压到 4.2pp —— **仍然有用，但不再是万能**。

**姿态（防御的零和重分配）**：硬化物理 = defP ×1.5 且 defM ×0.5，反之亦然。
关键在于**总量守恒**：选对赚到的，正好等于选错亏掉的 ⇒ 这套机制只改变**方差**，
不改变平均值。如果改成"只加不减"，那就是凭空加难度，三档基线会整体漂移且难以归因。

**落点**：`core.js` 的 `rawDamage()` / `stanceDef()` / `tagsOf()`、`data.js` 的 `STANCE`。

---

## 六、无头 AI：把"玩家"变成一个可复现的测量装置

平衡不能靠手感。`tools/sim.html` 用系统自带的 Edge 无头模式跑 900 局，
**和游戏共用同一份 `core.js`** —— 这是关键：早期用 Python 复刻过一遍核心公式，
两份实现一旦漂移，平衡数据就全是假的。现在物理上消除了这种漂移。

无头 AI 的判据与纪律：

- 它必须**能自己玩完一局**：会寻路、会打群战、会放魂技、会逛商店、会融合装备。
  （有断言专门检查"AI 会不会用某个新机制"—— 不进模拟器的机制，在平衡数据里等于不存在。）
- 它**不许绕规则**：比如"能不能放技能"由引擎的一个纯函数回答，
  界面和 AI 都问它 —— 如果界面自己判一遍，AI 就会绕过去，平衡数据当场变假。
- **作弊与卡死的判据必须分开**：一局里"连续 40 次没消耗回合"判 stall（AI 卡住，是 bug；
  会把整局作废）、"跑满 2400 回合"判 timeout（节奏太慢，是设计问题）。
  900 局必须 **0 stall / 0 timeout**，这是硬门禁。
- 它是**下限**，不是人类均值：它会按 fumble=0.2 犯错，但不会犹豫、不会手滑、也不会学。
  所以"人类真实通关率通常低于模拟数字"，这句话写进了 README。

**落点**：`tools/sim.html`、`core.js` 的 `_autoStep()` / `playHeadless()` / `simulate()`。

---

## 附：这些取舍带来的结果

| | 值 |
| :--- | :--- |
| 第三方运行时依赖 | **0**（渲染用系统自带的 WebView2；无框架、无构建步骤、无 npm） |
| 外部素材文件 | **0**（地形、角色、怪物、图标全部由代码或内联数据产生） |
| 发行包 | **677 KB**（解压约 2 MB，免安装） |
| 源码规模 | 11 个文件 / **13953 行** / 770 KB |
| 集成冒烟 | **300 项 ALL PASS**（真实页面 + 真实点击/拖拽 + 画布像素统计） |
| 平衡门禁 | **900 局 0 stall / 0 timeout** |

做法与流程（怎么保证这些改动不出错、AI 在其中扮演什么角色）见 [`工程方法.md`](工程方法.md)。
