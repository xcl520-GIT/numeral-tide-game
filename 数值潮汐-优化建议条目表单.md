# 数值潮汐 · 优化建议条目表单（供拍板）

> 来源：`数值潮汐测试反馈.txt`（玩家体验反馈，2026-09-23 20:54）
> 整理：2026-09-23 ｜ 对应版本：v11.1
> 说明：每条都**核对过代码**，标注了「玩家说的」与「代码里实际是什么」的差异。
> 工作量是估计值，不是承诺。每条末尾留了勾选位。

---

## 一、总览（先看这张）

| # | 反馈要点 | 对应玩家第几条 | 性质 | 工作量 | 牵连面 | 我的建议 |
|---|---|---|---|---|---|---|
| 1 | 区域间通路是**一格宽的单线**，绕过敌人容易被两头堵 | 1 | 生成器缺陷 | **小～中** | 地图生成 + 连通性断言 | ✅ **做**（先只加宽路） |
| 2 | 地图全黑却**看得清所有区域名**，迷雾失去意义 | 2 | 设计自相矛盾 | **小** | 区域标签渲染 | ✅ **先做**（半小时级） |
| 3 | 地图偏小、区域数量/种类少 | 2（末句） | 内容量 | 中～大 | 生成器 + 平衡 | ⏸ 缓做（依赖 1 的结论） |
| 4 | **战斗"没有操作感"**：回合制不像回合制、rogue 不像 rogue | 3 | ⚠️ **路线选择** | **大（等于重做）** | 战斗核心 + 敌人 AI + 平衡 | ❓ **必须你先定方向** |
| 5 | 潮汐压迫感不足 → 可以无痛"刷回合" | 3末+4末 | 平衡 + 机制 | **中** | 潮汐节奏 + 等待的代价 | ✅ **做**（性价比最高） |
| 6 | 职业不平衡：法师断档强、疾风最弱 | 4 | 平衡 | **中** | 职业数值 + 魂技 | ✅ **先量再改** |
| 7 | 层数少；Boss 除了更大更厚没特色 | 5 | 内容量 + 设计 | 中～大 | Boss 行为 + 层数配置 | ✅ 拆开做：Boss 特色优先 |
| 8 | 敌人种类少；物法抗区分没被感知；没人会去悬停看敌人信息 | 6 | 内容量 + 可读性 | 中（种类）/ 小（可读性） | 敌人表 + 提示机制 | ✅ 先做"可读性"，种类缓做 |

**如果只能做三件**：**2 → 5 → 1**。
理由：2 和 1 是"玩家一眼能看出变好了"的改动且风险低；5 解决的是**4 和 6 的共同温床**（见第三节）。

---

## 二、逐条

### 1. 区域间通路是一格宽的单线

**玩家说**：「区域与区域连接的路就是一条单线，绕过当前区域的敌人去下个区域很容易被两头堵。可以稍微提升一下地图的多样化、适当增大路的宽度。」

**代码里实际是**：
- 地图 45×35，13 个房间，房间 5~11 格（`data.js` 的 `MAP`）
- **走廊确实是 1 格宽**：`carveH` / `carveV` 只挖单行或单列（`core.js:327-336`）。全项目只有这一处挖路
- 岩浆从第 2 层起出现（`core.js:367`），并且已经有"应急通道"兜底开一条 L 型路（`core.js:534`）—— 说明作者早就知道连通性会被破坏

**判断**：玩家说得**准**。这是"单线"和"两头堵"的直接原因，而且是**参数级改动**，不是重构。

**若做要动什么 / 风险**：
- 循环多挖一行/一列（1 格 → 2 格），关键路口还要防止重新形成 1 格瓶颈
- 风险：冒烟里有**地图连通性**断言，还有"相机不变量"（视口四边必须被地形铺满）；水洼/岩浆/装饰的生成都假设"旁边有墙可贴"，加宽后这些参数要跟着调
- 验收：改完必跑 202 项冒烟 + 连通性抽样

**建议**：**拆分做**。先只做「路宽 2 + 路口加宽」，见效快、可回归；"每层换布局模板"放第二步。

- ☐ 采用（只加宽路）　☐ 采用（加宽 + 布局模板）　☐ 不采用

---

### 2. 地图全黑却看得清区域名（迷雾自相矛盾）

**玩家说**：「刚开局地图是黑的，但每个区域的名称却能看的一清二楚，这样给地图设置成迷雾状态感觉没啥必要。」

**代码里实际是**：
- 迷雾**真的实现了**：`explored` / `visible` 两个数组 + 视野计算（`core.js:292`、`687-712`）
- 但 `drawRegionLabels()` **完全不看迷雾**：它遍历**所有**区域，只按"是否访问过"分两档透明度（未访问 0.55、访问过 0.95）（`render.js:403-425`）

**判断**：这不是 bug，是**两套"已知"的口径没对齐** —— 区域是"上帝视角已知"，地砖是"需要探索"。玩家的人话翻译就是：既然全看得见，那黑着有什么意义？

**三个互相排斥的选项**：
- **A. 让区域名也吃迷雾**：只显示已探索到的区域。迷雾立刻有意义 —— 但**出口导航会一起变弱**（导航依赖区域信息），要联动改
- **B. 去掉迷雾**：开局全亮。最省事，但会失去"探索感"，而且与既有的地图设计意图冲突（视口故意小于地图、相机跟随）
- **C. 保留现状，但把"未探索"做得一眼可读**：未访问区域的标签更暗 / 加斜体 / 加模糊

**建议**：**先做 C**（半小时级，把矛盾显性化、风险最低）；A 作为下一步（要连带改出口导航）。

- ☐ A（区域名吃迷雾）　☐ B（去掉迷雾）　☐ **C（弱化未探索标签）**　☐ 不采用

---

### 3. 地图偏小、区域数量/种类少

**玩家说**：「建议可以增大一点地图，多设置一些新区域。」

**代码里实际是**：地图 45×35、13 房间；每层切 4~6 个区域，区域类型已有 **5 种**（普通/精英/危险/奖励/守门），每种带危险度星级。

**判断**：玩家要的具体是什么，有**两种读法**，需要你确认：
- 读法一 = 地图**更大**（尺寸/房间数）
- 读法二 = **区域类型更多**（新增第 6、7 种语义区域）

**若做要动什么 / 风险**：
- 增大尺寸会直接改变**每层的探索时长**，进而改变潮汐周期与通关回合数 —— **属于会动平衡的改动**，必须重跑 300 局模拟
- 新增区域类型要同时给：生成规则、地面染色、区域标签、危险度、以及"奖励区保底宝箱"这类配套约定

**建议**：**缓做**。它的收益依赖第 1 条的结论（先把现有地图的路网修好，再看是不是真的需要更大）。

- ☐ 增大地图　☐ 增加区域类型　☐ 都做　☐ 缓做　☐ 不采用

---

### 4. ⚠️ 战斗"没有操作感"——这是路线选择，不是优化条目

**玩家说**：「回合制玩法体验下来几乎感觉不到，更多的是 rogue 玩法，但没有 rogue 那种战斗感…数值比对面高就是直接秒杀…没有操作感。如果要走回合制感觉可以参考一下拉瑞安的作品，感觉最适合的是神界原罪2，所有操作都根据点数来实现，但是感觉有点复杂。如果走 rogue 玩法，就要新增普攻键位，敌人的行动逻辑就要改。」

**代码里实际是**：
- 战斗是**"接触即结算"**的抽象对决：走进敌人格 → 按 `攻击 × 50 /（有效防御 + 50）` 算一轮，物理/法术各算一遍、自动挑更高的那一路（`README` 第二节写得很清楚）
- 设计意图是**资源决策游戏**，不是战术游戏 —— 原话是「**你打得赢，不代表你打得值**」，压力来自"每场战斗都会掉血、而回血极少"
- 四个魂技是"时机按钮"，不是战斗系统

**判断**：这条**不是优化条目，是"这游戏要不要变成另一种游戏"**。而且玩家自己也意识到了矛盾（他给了两条互斥的路）。

**三条路（互斥）**：
| 方案 | 含义 | 代价 |
|---|---|---|
| **A. 保持"接触结算"，把决策做实** | 强化**战前**决策：该不该打、走哪一路、用哪个窗口。不改战斗核心 | 小～中。等于接受"操作感来自规划而非手感" |
| **B. 拉瑞安式回合战术** | 点数制、位置、行动顺序 —— 等于**重做战斗核心** | 大。敌人 AI、平衡、UI 全要重做 |
| **C. 动作化 rogue** | 加普攻键 + 改敌人 AI + 实时节奏 | 大。且与"回合制潮汐"的底层设定冲突 |

**我的判断（供你参考，不替你决定）**：
- 玩家的**真实痛点**可能不是"不够战术"，而是**"数值高就直接秒杀，所以没有过程"** —— 这在 A 下也能缓解（提高战斗轮数、让"打不划算"变得可感知）
- B / C 都是**换一个游戏**，不是改一个游戏。如果目标是"做一个能进简历的 AI 开发项目"，B/C 的投入产出比很差：成本高、周期长、且现有 202 项冒烟与 300 局平衡全部作废
- **但这是你的项目定位问题，只有你能定。** 如果目标是"做成一个真好玩的游戏"，B 值得考虑

- ☐ A（强化战前决策）　☐ B（点数制回合战术）　☐ C（动作化）　☐ 维持现状，不投入

---

### 5. 潮汐压迫感不足 → 可以无痛"刷回合"

**玩家说**：「潮汐的压迫感不强，所以可以很自在的刷回合」＋「潮汐的上升我感觉可以根据时间来涨」

**代码里实际是**：
- 潮汐**按回合**涨：`if (this.turn % this.diff.tideEvery !== 0) return;`，周期 `[0,1,2,3,3,2,1]`（`core.js:2003-2010`）
- 节奏按难度：休闲每 16 回合、标准每 12 回合、深渊每 8 回合
- 踩水掉血是**百分比**（`0.6% + 水位×0.5%` 最大生命/回合）
- 空格 = 原地等一回合，**等待没有任何额外代价**

**判断**：玩家的直觉是对的，但他给的药方（"按时间涨"）**方向可疑** —— 在一个回合制游戏里引入实时潮汐，会出现"你站着想事情，水就淹上来了"，那是另一个游戏。**真正的病根是：等待没有代价**。潮汐既然按回合推进，那"多等一回合"就该承担风险，而现在没有。

**这条为什么性价比最高**：它同时是**第 4 条和第 6 条的温床**——
- 法师能"远程来回刷回合无伤"、坦克能"刷回合用盾击杀"，都建立在"等待免费"上
- 把等待的代价做实，法师/坦克的强势会**自然收窄**，不必逐个职业调数值

**若做要动什么**：
- 让"原地等待"和"无意义回合"产生代价（例如：等待时潮汐额外推进、或等待累积惩罚、或敌人获得行动优势）
- 注意：这条**会动平衡**（300 局模拟必跑），而且会同时影响三档通关率

- ☐ 采用（让等待有代价）　☐ 采用（潮汐改实时）　☐ 两个都要　☐ 不采用

---

### 6. 职业不平衡：法师断档强、疾风最弱

**玩家说**：「法师断档的强，远程来回刷回合就能轻松无伤，坦克也可以刷回合来用盾进行击杀，其余两个有点牢，疾风更是牢中之牢，技能约等于没有。」

**代码里实际是**：
| 职业 | 关键区别 | 魂技 |
|---|---|---|
| 破军（重刃武士） | 物攻 44、血 130；被动：每杀 1 敌永久 +2 物攻 | 裂地斩（范围伤害，CD 10） |
| 秘仪（潮语术士） | 法攻 46、血 **96**（最脆）；被动：法术击杀回 10% 最大生命 | 潮语洪流（CD 9） |
| 疾风（潮痕游侠） | 速度 **26**、暴击 14%、闪避 8%；被动：每战第一轮必先攻且 +40% 伤害 | 疾影（CD 8、**不消耗行动**） |
| 铁壁（潮汐守誓者） | 血 168、双防 18；被动：血量 <50% 双防 +60%、每战结束回 8% | 不退之壁（双防 ×2.5、回 22% 血） |

**判断**：玩家**很可能说得对**，但目前只有他的体感，**没有数据**。而这个项目**恰好有分职业模拟器** —— 可以直接量出"法师到底强多少点通关率"。
**这件事应该先量、再改**：不量就调数值，等于把平衡数据作废。

**建议**：**先跑一次分职业模拟**（休闲/标准/深渊 × 4 职业 × 每档 100 局）。有了数字之后：
- 如果法师确实高出 10 个点以上 → 调数值（最省事、可回归）
- 如果差距不大 → 说明问题在"手感"而不是数值，应该回到第 5 条（等待的代价）
- 疾风的问题可能在**设计层**：它的被动"第一轮先攻"在"接触即结算"的战斗里几乎没有发挥空间，加个伤害/位移机制比调速度更有效

- ☐ 先跑数据再定　☐ 直接调数值　☐ 重做疾风的机制　☐ 不采用

---

### 7. 层数少；Boss 没特色

**玩家说**：「正常游玩层数有点少，外加 boss 的存在感不是很强…boss 可以做的更有特色一点，除了有名称和比较大外跟小怪没啥区别。」

**代码里实际是**：
- 层数：休闲 **3** / 标准 **4** / 深渊 **5**（`data.js` 难度表）
- Boss 有 3 种（潮汐之主 / 骨潮督军 / 渊喉），都是"更大的体型 + 更高的数值 + 一顶王冠"（`data.js` 的 `BOSSES`）
- **但其实有一个机制**：潮汐之主死后水位回落（`note` 里写了，`core.js:1505` 也有对应逻辑）—— 只是这个机制没被玩家感知到

**判断**：拆成两个诉求：
- **Boss 特色**：值得做，而且是"设计债"——3 个 Boss 的数值差异有，**行为差异没有**。做法是给每个 Boss 一个专属行为（比如督军周期性召唤、渊喉击退/吞地形、潮汐之主直接驱动水位）
- **层数偏少**：这是**难度配置问题**（标准 4 层）。加层数会拉长单局时长，也可能让中段变得重复 —— 而玩家第 1 条恰恰说"每层感觉大差不差"。**先解决"层与层不像"，再考虑加层**

**建议**：**先做 Boss 行为差异化**（其中"潮汐之主驱动水位"已有雏形，是现成的抓手），层数问题等第 1、3 条有结论再说。

- ☐ 做 Boss 行为差异　☐ 加层数　☐ 都做　☐ 不采用

---

### 8. 敌人种类少 / 物法抗没被感知 / 没人会悬停看敌人信息

**玩家说**：「敌人种类除了一些血比较多就没其他的…比如法师，弓箭手，召唤师。游戏内有物抗和法抗的概念，没有做好明显的区分就很容易死掉。在当前的游玩模式下很少时间会去用鼠标去显示敌人的信息。」

**代码里实际是**（这条最需要澄清）：
- 已经**不止是"血多血少"**，每个敌人的偏科很鲜明：

| 敌人 | 物防 | 法防 | 特殊 |
|---|---|---|---|
| 黏液怪 | 2 | 2 | 平庸杂兵 |
| 石甲兽 | **26** | **0** | 必须换法术打 |
| 幽魂 | **0** | **28** | 必须换物理打 |
| 疾风兽 | 3 | 3 | 速度 26，抢先手 |
| **孢子母** | 8 | 12 | **每 3 回合孵化一只黏液怪 —— 这就是召唤师** |
| **祷者** | 10 | 20 | **法术攻击 + 25% 吸血 —— 这就是法师** |
| 巨兽 | 15 | 11 | 血厚打得疼 |
| 镜影 | 19 | 19 | 全均衡无弱点 |
| 宝箱怪 | 22 | 4 | 必掉装备 |

- **没有远程单位**（弓箭手）：全部敌人都是贴身攻击
- **没有真召唤师之外的第二套 AI**：孢子母的孵化是唯一"会改变战场"的行为

**判断**：玩家说"种类少"**一半对一半不对** ——
- **不对的一半**：召唤师（孢子母）和法师（祷者）**已经存在**，物法抗的偏科也**做得很鲜明**（0 vs 26/28）
- **对的一半**：**玩家感知不到**。原因有三：① 祷者/孢子母权重低（5/4，而杂兵 10/8），出现频率低；② 这些敌人混在杂兵里，没有视觉/登场提示；③ **玩家根本不会去悬停看信息**（这句他自己说了）
- 所以真正的问题是**可读性**，不是种类数量：**"偏科"是这个游戏的核心机制，但它藏在悬停提示里**

**建议（按性价比排序）**：
1. **可读性优先（小改动、收益最大）**：让"该用哪一路"在画面上直接可见 —— 例如敌人格上直接显示弱点颜色/图标、或走近时自动弹出信息（而不是等玩家去悬停）
2. **提高已有特色敌人的权重**，让玩家真的遇到它们
3. **新增远程单位**：要动战斗模型（现在没有"距离"这个概念），属于大改动，缓做

- ☐ 做可读性（弱点直接可见）　☐ 提高特色敌人权重　☐ 新增远程/更多 AI　☐ 不采用

---

## 三、依赖关系（这张比单条更重要）

```
第 4 条（路线选择）
   ├─ 选 A（强化战前决策）→ 第 5、6、8 条按"轻量改动"做即可
   ├─ 选 B（点数制回合战术）→ 战斗核心重做，202 项冒烟 + 300 局平衡全部作废重来
   └─ 选 C（动作化）→ 同上，且与"回合制潮汐"的底层设定冲突
```

**关键洞察**：第 5 条（等待有代价）是第 4 和第 6 条的**共同温床**。
玩家说"法师能刷回合无伤"、"坦克能刷回合击杀"——**两个都是"等待免费"的产物**。
先把等待的代价做实，这两个职业的强势会自然收窄，不需要逐个调数值。

**所以顺序建议**：
1. **第 2 条**（半小时，零风险，玩家一眼看得出变化）
2. **第 4 条定方向**（你拍板，其余全部依赖它）
3. **第 5 条**（改一处、同时缓解 4 和 6）
4. 跑**分职业模拟**量化第 6 条 → 再决定要不要调数值
5. **第 1 条**（地图路网）
6. 第 8 条的可读性部分（小改动，但建议在方向定稿后做，避免返工）
7. 第 3、7 条（内容量类，放最后）

---

## 四、需要你先回答的三个问题

1. **这游戏要继续是"资源决策游戏"，还是转成"战术游戏"？**（第 4 条）
   —— 这一条决定后面全部条目的做法与成本，也是唯一一条**做错了会把项目带偏**的。
2. **"优化"的目标是什么？** 是「让人更愿意玩下去」，还是「让项目更像一份作品集 / 简历项目」？
   —— 两者的答案完全不同：前者要投入第 4、7 条；后者的话，第 2、5、8（可读性）这类
   "小改动大观感"的条目性价比高得多。
3. **你能接受每次改动都要重跑 202 项冒烟 + 300 局平衡模拟吗？**
   —— 这是这个项目已经建立的验收纪律。凡动模型层（潮汐、职业、敌人、地图）的条目，
   都必须付这个成本。可以接受的话，第 5、6 条就能做得稳；不接受的话，建议只做第 2、8 的渲染层改动。

---

## 附：我核对过的代码位置（便于你或我复查）

| 结论 | 位置 |
|---|---|
| 走廊只挖 1 格宽 | `src/js/core.js:327-336`（`carveH` / `carveV`） |
| 地图 45×35、13 房间 | `src/js/data.js:474`（`MAP`） |
| 岩浆自第 2 层出现 | `src/js/core.js:367` |
| 应急通道兜底 | `src/js/core.js:534` |
| 迷雾数组与视野 | `src/js/core.js:292`、`687-712` |
| 区域标签**不检查迷雾** | `src/js/render.js:403-425`（`drawRegionLabels`） |
| 潮汐按回合推进 | `src/js/core.js:2003-2010`（`_tideTick`） |
| 难度层数与潮汐周期 | `src/js/data.js` 难度表（休闲 3/16、标准 4/12、深渊 5/8） |
| 四个职业与魂技 | `src/js/data.js` 职业表 / 魂技表 |
| 敌人偏科与孢子母/祷者 | `src/js/data.js:375-422`（`ENEMIES`） |
| 三个 Boss | `src/js/data.js` 的 `BOSSES` |

---

# 表单 v2 增补（2026-09-24 ｜ 对应 v11.3）

> v2 **不重排** v1 的条目，只在后面追加两样东西：
> ① v1 里被**实测修正**过的判断；② 这一轮新长出来的问题。
> 每条独立、可单独拍板。「共用改动」栏标出"改了它就会同时影响谁" ——
> 那决定了改动顺序，比工作量更重要。

---

## A. v1 条目的实测修正

| v1 # | v1 的说法 | 实测后应该改成 |
|---|---|---|
| **4** | 战斗「没有操作感」（判断：路线选择，等于重做） | **拆成三件可分别验收的事**（A1/A2/A3）。第一件已完成：引擎不再替玩家选物法 + 逐回合菜单 + 姿态在战斗内轮换。**但它现在还不值多少** —— 因为 83% 的仗只有一轮。 |
| **5** | 潮汐压迫感不足 → 可无痛刷回合 | **确认成立，本轮没动。** 潮汐仍然只是 HUD 上的一个数字，"原地等一回合"没有任何空间代价。 |
| **8** | 物法抗区分没被感知 | **本轮解决了大半**：菜单直接给入参（攻 / 穿 / 姿态调整后的防），并且冒烟有一条断言**禁止**菜单出现"建议 / 推荐 / 最优"。 |
| **6** | 职业不平衡（法师断档强、疾风最弱） | 仍成立。本轮没碰任何数值。 |
| **2** | 迷雾与区域标签矛盾 | ✅ 已在 v11.2 期间修掉（标签走迷雾），本条可以划掉。 |

---

## B. 新条目

### A1. 让战斗长到 2~3 轮 —— **最高优先**

**现象**（`tools/probe_fightlen.html`，n=60 标准）：

| 敌人类型 | 均轮数 | 一轮结束占比 |
|---|---|---|
| 普通怪 | 1.09 | **92%** |
| 精英 | 1.49 | 66% |
| 首领 | 2.34 | 37% |
| **全体** | **1.24** | **83%** |

**为什么它排第一**：它是**决策层的天花板**。
任何"每轮做一次决定"的机制 —— 刚做完的物法菜单、将来要加的魂技与撤离 ——
在 1 轮战斗里都只能生效一次。菜单现在是为那 17% 的长战斗做的。

**共用改动** ⚠ 与 A2 强耦合：战斗一长，姿态在战斗内翻转的次数就跟着变，
`D.STANCE.every` 必须一起重标，否则会出现"打 8 轮才翻 2 次"这种节奏。
**两件事必须一起测。**

**牵连面**：三档平衡基线**必然整体重排**。这是它唯一的代价，也是它排在最前的原因 ——
它是这几条里唯一"改动小、只需重跑基线"的活。

**做法建议**：先只动**普通怪**（让杂兵也吃得住 2 轮），精英与首领暂时不动。
这样基线的移动只来自一个变量，归因清楚。可调的量：
双方 HP 曲线 / `COMBAT.K` / 普通怪的 `atkMul`。

**工作量**：中。改动本身是数值，工作量在验收（300×3）与重新定"一轮"的观感（演出时长）。

---

### A2. 战斗内姿态的翻面节奏（依赖 A1）

**现状**：`STANCE = { every: 3 }`，一轮结束推进一次 ——
只有打到第 4 轮以上才会中途换题目。

**判断**：这个数字是给"地图上按回合数轮换"定的，不是给"战斗内按轮数"定的。
A1 做完之后必须重新标定。

**工作量**：改一个数（小），但**验收成本等于 A1**（要重跑基线）。

---

### A3. 魂技进战斗菜单（独立，可先做）

**现状**：菜单里只有 物理 / 法术 / 托管。

**为什么没做**：`useSkill()` 是在**地图层**结算的（读 `this.px/py`、写 `this.hp`），
而对决进行中 `this.hp` 还没被 `_fightSettle` 写回 ——
两套状态源会同时写同一个敌人。那正是这一路一直在踩的坑。

**做法**：把 skill 的结算搬进对决层。`duel` 已经是可中断的结构，容器现成；
把"用魂技"定义成一种回合行动：本回合不出手，改放技能。

**共用改动** ⚠ 与 A4、以及 Phase 2 横板**共用同一个菜单容器**。
建议先把菜单改成"可注册动作"的形式，后面两项就只是往里加条目。

**工作量**：中。真正的风险不在代码量，而在语义 ——
技能原本是**范围**型（裂地斩打一圈、潮语洪流打一片），
而一场对决只有两个单位，得重新定义哪些技能在战斗里成立、以什么形式成立。

---

### A4. 撤离 / 退出战斗（独立，但必须先定价）

**为什么不能白做**：撤离如果没有代价，所有战斗都可规避 ——
击杀数、掉落、以及"未击败守卫时楼梯封锁"这三套系统会一起失去意义。

**需要的代价**（三选一或组合）：扣潮汐水位 / 扣回合（等于让潮汐涨）/
本次战斗掉落减半 / 若干回合内不能再进同一只怪的格子。

**共用改动** ⚠ 与 A3 共用菜单；与 A1 无关，可以并行推进。

**工作量**：中。定价需要单独一轮平衡测量（"能撤"会改变 AI 的目标选择策略，
模拟器 AI 也得跟着会撤，否则数据与真人脱节）。

---

### A5. 随从 / 站位 —— Phase 2 横板的前提

**现状**：`grep summon|ally|随从|companion` 全空。玩家 1 人 vs N 怪，
横板的"左右站位"目前只有一个单位可站 —— 阵型这个维度是空的。

**判断**：这不是"把画面改成横板"就能补出来的，它需要一套新的实体
（随从/召唤物）、目标选择规则、以及 AI 的取舍。
属于 Phase 2 的**内容层**，不是表现层。

---

## C. 依赖顺序（照这个顺序做，归因最清楚）

```
A1 战斗长度 ──▶ A2 姿态节奏          必须一起测，基线整体重排
A3 魂技进菜单 ─┐
A4 撤离      ─┼──▶ 菜单容器改造 ──▶ Phase 2 横板（站位 + 随从 = A5）
               │
（A3 / A4 可并行，与 A1 互不影响）
```

**如果只能做一件：A1。**
理由：其余每一项的价值都被它卡着 —— 菜单、魂技、撤离在 1 轮战斗里都只能生效一次。
它是这一层的天花板，而它恰好是**改动最小、只需重跑基线**的那种活。

---

## D. 本轮已修 / 已确认（不必再做）

| 项 | 状态 |
|---|---|
| v1#4 之一：引擎替玩家选物法 | ✅ v11.2-e 拆掉自动择优，改成玩家自己选 |
| v1#4 之二：逐回合指令菜单 | ✅ v11.3-b，每一轮停下来选一路 |
| v1#4 之三：敌人姿态轮换 | ✅ v11.2-b（按地图回合）+ v11.3-b（补上战斗内轮换） |
| v1#8：物法抗无感知 | ✅ 菜单只出入参；冒烟断言禁止出现结论 |
| 战斗场景表现（背景 / 构图 / 立绘 / 演出） | ✅ v11.2-h ~ v11.3-c；战斗立绘改为 96×96 重绘 |
| v1#2：迷雾与区域标签矛盾 | ✅ v11.2 期间已修 |
