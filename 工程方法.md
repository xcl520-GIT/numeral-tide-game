# 工程方法：让每一处改动都能被验证

> 这份文档不介绍游戏，只介绍**做法**。它回答两个问题：
>
> 1. 个人项目、没有团队、没有 CI，怎么保证改动没改坏？
> 2. 用了 AI 之后，怎么保证 AI 不会把项目带进沟里？
>
> 每个结论都对应仓库里可复现的东西：脚本、原始输出、复现命令。

---

## 一、先造尺子，再写玩法

一个人做游戏，最大的风险不是"功能写不出来"，而是**改坏了不知道**：
平衡、手感、AI 行为都是涌现出来的属性，肉眼看不出回归。
所以这个项目最早投入的不是玩法，而是**量具**：

| 门禁 | 防的是什么 | 怎么跑 | 现状 |
| :--- | :--- | :--- | :--- |
| 集成冒烟 `tools/smoke.html` | 界面 / 交互 / 渲染 —— 逻辑模拟器碰不到的那一半 | 把**真实页面**塞进 iframe，驱动真实点击与拖拽，并对画布做像素统计 | **300 项 ALL PASS** |
| 平衡模拟 `tools/sim.html` | 数值回归 + AI 行为死循环 | 无头跑 900 局，**与游戏共用同一份 `core.js`** | **0 stall / 0 timeout** |
| 语法自检 `tools/loadcheck.html` | 改坏一个文件后连报错都看不清 | 逐个解析并定位到行号 | 12 / 0 |

三个设计决定值得单独说：

- **模拟器不重写逻辑。** 早期用 Python 复刻过一遍核心公式，两份实现一旦漂移，
  平衡数据就全是假的。现在直接驱动同一份 JS —— **这是物理上消除漂移，不是靠纪律**。
- **所有测试固定 seed。** 第一版没固定，于是"画面亮度"在两次运行间从 25.3 跳到 79.2 ——
  那不是回归，是开局随机到的地图不一样。**不可复现的测试等于没有测试。**
- **冒烟能对"发包那一份"跑。** 加 `?page=.../release/数值潮汐/www/index.html`
  就对分发包副本跑同一套 300 项 —— 只有它绿灯，才代表"发出去的那份是好的"。

---

## 二、四条纪律（每条都配真实案例）

### 1. 跑前写死判据

不许"跑完看看数字再说"。改之前先把它要满足的条件写下来，跑完只做判决。

> **案例（v11.11 远程怪）**：改之前写下"三档通关率偏移 ≤ 4pp、第 1 层死亡 ≤ 10%"。
> 第一版跑出来第 1 层死亡 深渊 **+14.2%**，超了。
> 于是**改方案去凑判据**（砍出现权重与每击），而不是回头把判据改宽 —— 两轮之后落到 ±1.1pp。

### 2. 逐位比对

纯结构改动（不改变行为的重构）的验收标准是**数字一个字节都不变**，
不是"看起来合理"。这是唯一能证明"重构没改行为"的办法。

> **案例（v11.11 `waitcost` 转正）**：这条机制对真人有效、对无头 AI 无效。
> 所以它的验收写的不是"数字变好了"，而是"**数字逐位不变**"——
> 那才证明它没偷偷碰到别的东西。

### 3. 可选字段 = 天然开关

新机制一律写成"字段不写就是旧行为"。好处是三重的：
能单独回退、能和上一版逐位对照、能随时做 A/B。

> **案例**：`range`（远程怪射程）、`penPerDepth`（敌人穿透）、`speedGap`（速度碾压阈值）、
> `deepCorrupt`（深层潮水）、`MAP.pillarStep`（柱厅石柱密度）—— 全是一个字段，
> 删行即回退。文档里每一项都写了"回退方式"。

### 4. 难度变更必须记账

动难度不算错，**不记账**才算错。每次难度变更都要给出 n=900 的前后对照，
并把净结果拆开。

> **案例（v11.10 深渊）**：改了补给与深层潮水两项，净结果是 −1.0pp。
> 记账把它拆成 **+1.7pp（第 2 项）与 −2.7pp（第 3 项）**——
> 否则下一个人只会看到"深渊平白难了 1 个点"，永远不知道该找谁。

---

## 三、证伪比证实更值钱

这个项目里被数据**否掉**的方案，比落地的方案多。被否掉的写进记录，也是资产 ——
它同时交代了"为什么最后不是那么做的"。

| 被否掉的方案 | 为什么当初看起来合理 | 数据怎么否的 | 换成了什么 |
| :--- | :--- | :--- | :--- |
| 削减怪物数量来降低第 1 层压力 | 怪少了自然更松 | 死亡数几乎不动，通关率却塌到 15% ——**敌人也是资源**（每 4 杀升级、每 6 杀秘藏） | 补给（回血比例） |
| 关掉精英怪 | 精英是主要死因 | 死亡数反而更差 | 同上 |
| 抬高怪物攻击力，把压力推给后半程 | 攻击高就该更难 | 第 5 层 atk ×4.15，伤害占血只从 0.1% → 0.6% —— 伤害公式是**饱和**的，抬 atk 先杀低防玩家 | 深层潮水（按最大生命扣血、无视防御） |
| 复活流（死了能接着打） | 延长局内寿命 | 通关率越档 35.3%、总死亡不守恒、回合翻倍 —— **那是换游戏，不是调难度** | 把死亡从前两层"搬"到第 3-4 层 |
| 只加宽走廊口子 | 直击玩家抱怨的"两头堵" | 群战发生率不动 | 整条路宽 2 |
| 让 AI 盯"敌人姿态翻面"来赚等待收益 | 姿态每 3 回合翻面，等到软面再打 | 等待占比 **0.0~0.1%**，等于从未触发（姿态只影响敌人防御，而玩家每轮本来就能自己换路） | 盯魂技冷却 |
| 让 AI 盯"魂技冷却"来赚等待收益（第二版） | 冷却是全游戏唯一随回合变好的量 | 等待占比 0.2~1.0%，仍近零；**把门槛拆开量**：等待占比涨 6 倍而通关率不动 | 结论："等"本身没有收益 —— 于是解冻了被它冻结的整条职业线 |
| 按职业强度矩阵调职业数值 | 数据摆着：四职业极差 52.6pp | 玩家感知与数据**完全相反**（玩家说"疾风牢中之牢"，数据里它第二强）。根因是**模拟 AI 只会贴脸、不会风筝** —— 量具量不到玩家在做的事 | 不改数值；先给模拟器补"风筝"策略 |
| 远程怪按 n=300 的数据调代价 | 数据说代价太大 | n=900 才看出前两轮几乎白调（**往敌人表里加条目会重排随机流**，改动前后不是同一批世界） | 把验收口径改成 n=900 才算尺子 |

---

## 四、我和 AI 的分工

这一节是这份文档的重点：**AI 在这套流程里干什么、我不让它干什么。**

| 环节 | 我 | AI |
| :--- | :--- | :--- |
| 方向与取舍（要不要做、做哪个） | **定** | 提供选项、代价与反例 |
| 判据（什么算通过） | **跑前写死** | 只负责执行 |
| 取证（数据从哪来） | 要求可复现、要留原始输出 | 写探针、批量跑、整理成表 |
| 判决（过还是不过） | 看数字定 | **不许自评通过** |
| 文档 | 定结构与口径 | 起草、整理 |

为什么这样分：AI 最擅长的是**跑量、写脚本、整理信息**；最不可靠的是
**在模糊判断上给自己找台阶**。所以凡是能变成"可执行门禁"的判断，都不交给讨论。

它的效率是真的：一轮 900 局平衡模拟约 3 分钟，冒烟 300 项约 1 分钟 ——
**改一次、跑一轮、看数据**这个循环能一天转几十遍。没有这个循环，
上面那份"证伪清单"根本写不出来（人肉跑 2700 局不现实）。

---

## 五、挡 AI 幻觉的七条机制（每条都真的踩过）

1. **只认可执行的门禁，不认"我看过了"。**
   任何结论都必须能落到一个脚本上，并把原始输出留档。
2. **固定 seed + 逐位比对。**
   事故：测试没固定 seed，"画面亮度"两次运行从 25.3 跳到 79.2 ——
   差点被当成回归去修一个不存在的问题。
3. **断言从数据表现读，不写死参数。**
   事故：断言里写死了"深层潮水扣 6%"，参数一调到 10.5% 断言就 FAIL ——
   而它报出来的样子和"机制坏了"**一模一样**（是断言替数据说话，不是数据替断言）。
4. **探针必须能独立复现，而且要能提前退出。**
   事故：900 局里只有 1 例卡死。让探针"找到就退出"，扫描只用 9 秒，
   于是可以拿着那一局的种子反复复现、插桩、定位；整轮模拟要 170 秒，不可能这么用。
5. **要求证伪，而不是要求确认。**
   "帮我证明这个方案可行"会得到一份论证；"帮我**否掉**这个方案"才会得到数据。
   上面第三节那 9 条，绝大多数是这么来的。
6. **测试桩必须不对称。**
   事故：要验证一个兜底会不会触发，我把判据里"前后两次采样"用同一个偏移量改掉，
   两次偏移互相抵消，那条断言**白测了一次**。
7. **不拿环境的巧合当断言。**
   事故两次：断言"这张图上一定找得到一个被墙挡住的落点"（FOV 是 240 条射线的扇形，
   射程内几乎处处可见）、断言"把石柱密度调满就能逼出兜底"（别的格替这一区保住了通路）。
   地图是随机生成的 —— 这类断言会时绿时红，等于没测。**要测就主动构造条件。**

**还有一条元规则**：当讨论陷入"我觉得可以"的时候，把判决权交回**跑前写死的数字**。
AI 和人都会自我说服，数字不会。

---

## 六、这套做法换来了什么（事实清单）

- 抓到一个**只在 2700 局里出现 1 例**的死循环，并定位到具体种子：
  根因是"玩家与精英怪互相追出来的两格横跳"（两个局部最优凑成一对完美周期）。
  修法沿用同一条既有纪律（净位移为零的往返连续 3 次 → 按"走不到这个目标"处理）。
- 顺着一条"数据看着没问题但解释不通"的线索，挖出**一行兜底 `penP/penM = 0`**
  造成的系统性设计缺陷：**玩家堆防御没有对手**。并给出了"用穿透而不是抬攻击"的解法
  及其实测（装备防御的价值 6.9pp → 4.2pp：仍然有用，但不再是万能）。
- 识别出最隐蔽的一类坑 —— **量具本身有问题**：职业强度矩阵与玩家感知完全相反。
  处理方式是**拒绝按错的数据改数值**。
- 用一轮 900 局对照发现"内容新增会重排随机流"，从而把验收口径从 n=300 改成 **n=900**。
- 冒烟从 290 项涨到 300 项，新增的每一条都对应一个"讲得通但测不出来"的失效方式。
- 31 份研发记录：立项 → 跑前判据 → 取证 → 落地或证伪 → 记账 → 留证。

---

## 七、自己验一遍

```powershell
cd numeral-tide/数值潮汐
$edge = "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe"

# 300 项集成冒烟（真实页面 + 真实点击/拖拽）
& $edge --headless=new --disable-gpu --allow-file-access-from-files `
  --dump-dom "file:///<绝对路径>/tools/smoke.html"

# 900 局平衡（与游戏共用同一份 core.js；看末行 winRate 与有无 STALL）
& $edge --headless=new --disable-gpu --virtual-time-budget=500000 --dump-dom `
  "file:///<绝对路径>/tools/sim.html?n=900&diff=all&cls=0"
```

`s�im.html` 的每个开关都能单独回退（`?bossfx=0` / `?waitcost=0` / `?roadw=1` …），
`data.js` 里还有一组"不在 sim 里的旋钮"，文档里都标了删哪一行能回退。
