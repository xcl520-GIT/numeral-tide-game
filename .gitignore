# ============================================================
#  数值潮汐 · 忽略规则
#
#  原则：**构建产物不入库**。
#  www\ 是 build.ps1 从 src\ 复制出来的，exe 与 DLL 是编译/第三方产物，
#  它们的唯一来源都在仓库里 —— 提交它们只会让 diff 变得没法看，
#  而且每次构建都会产生一堆二进制变更。
# ============================================================

# ---- 构建产物 ----
数值潮汐/www/
数值潮汐/数值潮汐.exe
数值潮汐/*.dll
数值潮汐/说明.txt
数值潮汐/release/数值潮汐/
数值潮汐/release/*.zip

# ---- 素材源包 ----
# 4200+ 个 SVG（game-icons.net，CC BY 3.0）。实际用到的图标已经内联进
# src/js/icons.js，所以这里只是"来源存档"，不入库。
# 出处与许可证见 数值潮汐/assets-raw/README.md。
数值潮汐/assets-raw/game-icons/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
*.swp
Thumbs.db
Desktop.ini
*.log

# ---- Python ----
__pycache__/
*.pyc

# ---- 开发过程中的临时文件（不入库）----
# 一次性取证脚本与原始输出：证据留在本地，结论与复现方式写进 开发记录\
_probe/
# 下划线开头的草稿 / 片段
开发记录/_*
