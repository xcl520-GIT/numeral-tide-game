# v11.5 工作单建议（2026-09-24）

> 依据：`数值潮汐-优化建议条目表单.md`（v1/v2）、`v11.4-n-群战站位三次证伪.md`、
> `平衡基线-20260924-v11.4-f.md`、`数值潮汐测试反馈.txt`，并重新核对了
> `core.js`（v11.4-k 之后的 HEAD）与 `data.js`。
> 行号一律不作锚（v11.4 改动后已漂移），全部用函数名/标识符定位。
> 每条给出：**证据 → 做法 → 验收 → 回滚方式**。

---

## 0. 现状定位（截至 v11.4）

**已完成、不必再碰**：
- 物法由玩家自己选（v11.2-e）、逐回合指令菜单（v11.3-b）、姿态战斗内轮换 + `STANCE.every` 3→2（v11.3-b / v11.4-f）
- A1 战斗长度：野生普通怪 2.44 轮 / 一轮结束 28~29%（休闲·标准达标；深渊 43% 不达标，根因见 P5）
- 群攻三形态（v11.4-k）、迷雾与区域标签矛盾已修（v11#2）

**有证据但还没做的债**（本单要处理的）：
| 编号 | 事项 | 证据来源 | 状态 |
|---|---|---|---|
| P0 | 分职业平衡只有体感、没有数据 | v1#6 | 两轮未跑 |
| P1 | 等待零代价 → 法师/坦克刷回合 | v1#5，v2 表 A 段"本轮没动" | 两轮未动 |
| P2 | 群战 19% 需拍板 + 通路一格宽 | v11.4-n 三次证伪 / v1#1 | 停在选择题 |
| P3 | 魂技不进菜单、无撤离 | v2 表 A3/A4 | 未做 |
| P4 | Boss 无行为差异；深渊开局死亡率过高 | v1#7 + 基线"结束层分布" | 未做 |
| P5 | 装备主导的雪球（深渊一击 203 vs 杂兵血 200~240） | v11.4-f 残留判定 | 明确"单独一轮" |

**一个新观察（原始材料里没人点破）**：v11.4-f 深渊结束层分布 `{1:157, 2:21, 3:3, 5:119}` ——
300 局里 157 局死在第 1 层。深渊的难度不是"整体偏难"，是**顶部过重**：
过半玩家根本走不到第 2 层的精英和第 5 层的 Boss。这让 P4（Boss 特色）和
"加层数"（v1#7）在当前深渊面板下都是**给到不了那一步的人做的功能**。
任何深渊改动跑完，先看这个分布有没有从 157 往中间挪。

---

## P0 · 分职业量化（零风险，先跑，产出决定后面往哪打）

**证据**：v1#6 挂了"先跑数据再定"，两轮没人跑。玩家断言法师断档强、疾风最弱。

**做法**：纯采集，不改代码。命令与 v11.4-f 基线同口径：
```
tools/sim.html?n=300&diff=standard&cls=<1..4>   （每职业独立跑，含休闲/深渊则 ×3）
tools/sim.py 同种子（1000+i*7919、fumble=0.2）交叉复核一档
```
**每职业要记录的指标**（不只是通关率）：通关率、平均每局承伤、分职业战斗轮数、
以及**每局原地等待回合占比**（模型层 `wait` 分支加一个计数器，P1 要用它当分母）。

**判定规则（跑之前先定死，防止事后挑数据）**：
- 职业间通关率极差 > 15pp → 走数值调整（改面板，最小改动）
- 极差 ≤ 15pp 但疾风的"等待占比"和"菜单使用轮数"显著低 → 问题在机制不在数值，
  直接接 P1 + P3（疾风弱的真实原因大概率是被动"每战第一轮先攻 +40%"在接触结算里
  只值第一轮的一点伤害，玩家感知不到）
- 疾风的候选机制修正（二选一，跑完数据再拍）：
  a) 先攻附带"本轮可脱离战斗一格"（把速度变成生存资源，和 P3 撤离共用容器）
  b) 第一轮命中后，本场对决每轮多打 1 次（把一次性优势变成持续优势）

**验收**：一张 4 职业 × 3 难度的矩阵表进开发记录。**回滚**：不涉及，纯读。

---

## P1 · 让等待有代价（表单两轮点名"性价比最高"，仍未动）

**证据**：v1#5；v2 表 A 段原话"潮汐仍然只是 HUD 上的一个数字，原地等一回合没有任何
空间代价"。玩家原话"法师远程来回刷回合就能轻松无伤"——这条不修，P0 调完数值
法师照样能刷，等于白调。

**做法**（推荐 b，改动最小且不动潮汐模型）：
- a) 等待额外推进一个潮汐节拍（`_tideTick` 入口：等待时 `turn+1` 再判周期）
- b) **等待累积惊动**：每次原地等待，给半径内所有已醒敌人一次"免费挪一步"
  （复用 v11.4-i 已实现的 alerted 移动逻辑，那条代码路径现成）
- 不推荐"踩水加倍掉血"：那是惩罚站位而不是惩罚等待，会误伤合法的退潮窗口行动。

**联动改动（必须做，否则数据和真人脱节）**：模拟器 AI 目前不刷回合，所以现在的
通关率没吃到这条红利。给 AI 加一条"低血量且安全时等待"的策略，重跑后
**基线会掉是正常的**，掉在法师/坦克身上才是目的达成了。

**验收**：
- 300×3 重跑：标准档通关率允许从 48% 掉到 40~45% 区间
- 关键指标：真人式 AI 的等待回合占比从"无约束"降到 <8%
- 分职业极差比 P0 实测值收窄 ≥ 一半
**回滚**：单开关 `?waitcost=0/1`，跑 A/B 对照（项目规矩，三次证伪都带对照组）。

---

## P2 · 空间改造轮：路宽 2 + 守门区堆（一次解掉 v1#1 和群战拍板）

**证据**：
- v1#1：`carveH/carveV` 只挖单行/单列，通路 1 格宽；玩家原话"绕过当前区域很容易被两头堵"
- v11.4-n：行为侧杠杆全部证伪，唯一未试方向 = "把堆放在玩家必须穿过的地方"，
  等拍板；且第五次读数指出"堆不是围不上，是没跟上来"——玩家撞的是前面落单的怪

**关键洞察**：这两条是同一枚硬币。1 格走廊既是"两头堵"的原因，也是"群战凑不齐"的原因
（窄口上 4 只永远站不进四邻）。修路网和修群战必须同一轮做，拆开做会互相污染数据。

**做法**：
1. 路宽 1→2：`carveH/carveV` 参数化多挖一行/列；关键路口（两走廊交汇处）扩成 3×3
2. 堆布点改必经之路：精英/守门区的堆生成点从"房间内随机"改为"贴出口/窄口"
   （布点函数在区域划分之后、`_squad` 相关生成处；`tools/packcheck.html` 是现成的堆形取证）
3. 分层门控：第 1 层守门区仍单堆（开局层是教学，v11.0 已定的原则），第 2 层起必经堆

**注意边界**：这不触碰你自己划的"群战触发规则不能动"——触发判据一字不改，改的是布点位置。

**验收**：
- `probe_benchsim / probe_fightlen` 同尺子（n=200 standard）：群战 19% → **≥40%**（这是 v11.4-k 立的达标线）
- 202 项冒烟全过（重点：地图连通性断言、相机不变量——路宽 2 后视口边缘覆盖要复核）
- 通关率三档波动 ≤ ±8pp（若超，先查是否加宽让"绕怪"变得太容易，那要回调堆位而不是回退路宽）
**回滚**：两个独立开关 `?roadw=1/2`、`?packanchor=0/1`，可分别归因。

---

## P3 · 菜单容器"可注册动作"改造：魂技入战 + 撤离一起做

**证据**：v2 表 A3/A4。现在普通怪 2.44 轮（P 前置的 A1 已完成），菜单第一次有了用武之地；
但魂技仍在地图层结算（`useSkill` 读写 `this.px/py/hp`，与 `_fightSettle` 双状态源——
这个坑 A3 条目里写得很清楚），撤离还没做。

**做法**：
1. 先把 `_fightTurn` 的选项从硬编码（物/法/托管）改成动作注册表：
   `{ id, label, enabled(), execute(duel) }[]`——这是 A3、A4、未来 A5 共用的容器，先改它
2. 魂技进对决（语义要重新定义，因为一场对决只有两个单位；下面是**供拍板的示例值**，不是结论）：
   - 裂地斩（burst）：本轮伤害 ×1.6，但本轮自身受伤 ×0.5（重击的代价写成姿态）
   - 潮语洪流：本轮无视姿态按对方弱化侧结算（配合 `STANCE` 语义）
   - 疾影：本回合额外一次出手、不耗回合——给疾风"菜单里的存在感和对价"
   - 不退之壁：本轮免伤 + 40% 反伤
   每个技能在 `duel` 里只允许用一次，CD 语义从"地图回合"映射为"战斗轮"。
3. 撤离（定价三件套，A4 说的"必须先定价"，推荐组合）：
   - 本场战斗掉落减半 + 水位立即推进一节拍 + 该敌人保留残血（再进战从当前血继续）
   - 模拟器 AI 同步加 flee 判定（预期本轮承伤 > 剩余血量 30% 时撤），否则数据失真

**验收**：202 项 + 新增断言（菜单里每个注册动作至少有 1 条冒烟真按下去）；
三档 300 局重跑；人工看 `probe_shot` 战斗截图确认菜单不溢出。
**回滚**：魂技/撤离各自是注册表里的独立条目，注释掉注册即可回滚。

---

## P4 · Boss 行为差异化（三个全部复用现成代码路径）

**证据**：v1#7"除了名称和比较大跟小怪没区别"。现状 3 个 Boss 只有数值差；
潮汐之主"死后水位回落"有雏形但玩家感知不到。基线的结束层分布显示
大量玩家死在第 1 层——Boss 问题要和"前两层体验"一起看。

**做法**（每个都是函数级复用，不是新系统）：
- **潮汐之主**：补上"活着 = 涨潮加速"的前半段——它在场时 `_tideTick` 周期缩短一档，
  死亡回落逻辑已有。水位数字 + 屏幕染色 + 一句横幅（"潮水受它驱使"），让玩家**看见因果**
- **骨潮督军**：直接复用孢子母的 `spawn: {every, count}` 机制，每 4 回合召 1 只幼体；
  击杀 Boss 本体即停止召唤（给玩家"断源优先"的决策）
- **渊喉**：复用裂地斩已有的击退+震晕，反向作用于玩家：每 3 轮一次"吼"，
  把玩家推开 1~2 格并打断当前移动意图（不造成伤害，纯空间惩罚）

**验收**：`probe_fightlen` Boss 均轮数 2.34 → ≥4；人工通关一次深渊第 5 层截图存档。
**回滚**：行为挂在敌人模板的一个 `bossBehavior` 字段上，置空即回旧行为。

---

## P5 · 深渊装备雪球重标定（单独一轮，别和上面混）

**证据**：v11.4-f 残留判定的原话：深渊 55.7 杀 → 8.7 件秘藏 → 每击 203 点 vs
第 5 层杂兵 200~240 血，一轮结束 43% 压不下去是因为**后期玩家面板碾压**，不是曲线没调够。

**做法**（两个候选，先跑 P0 数据再选）：
- a) 装备加成递减：同类 mods 第 2 件起 ×0.85/件（改 `stats()` 聚合处，一处代码）
- b) 秘藏"永久 > 一次性"基线不动，但**提高深渊后层杂兵血线**（数据表改动，简单粗暴）

推荐 a：它治的是雪球本身，b 只是把墙加高、又会把轮数拉长反噬 A1 的达标线。

**验收**：深渊每击伤害分布（探针加一列）、一轮结束 43% → ≤35%、
结束层分布第 1 层死亡数 <120（P0 里那个 157 的观察指标）、通关率回到 ±10pp 带内。
**回滚**：`?equipdecay=0/1` 开关。

---

## 依赖与排期建议

```
P0（跑数，1 天）─┬─▶ P1（等待代价，用 P0 的等待占比当基线）
                 └─▶ P5（选 a/b 要等职业数据）
P2（空间轮）独立可做，但要在 P1 之前或之后单独跑，不要同一轮两个变量
P3 依赖 A1（已完成）；P4 依赖 P2 与否不强求，但 P4 的督军召唤会轻微稀释群战率，P2/P4 同轮要分开关
```

**若只做一轮：P0 + P2 + P1。**
理由：P0 是纯采集、直接产出下一切争议的裁判；P2 一次解掉两个挂了三轮的问题
（v1#1、群战达标线）；P1 是所有"刷回合"类抱怨的共同上游，先修它，
职业数值（P0 的产出）才不会被刷回合行为重新污染。

**验收纪律沿用项目现状**：凡动模型层 → 202 项冒烟 + 300×3 基线重跑 + 独立归因开关。
建议把四个常驻指标钉进每轮基线，形成趋势线而不是每次重新解释：**一轮结束占比 /
群战率 / 等待回合占比 / 深渊第 1 层死亡数**。
